// import * as jwt from 'jsonwebtoken';

// export const generateToken = (data: any, expiresIn: string) => {
//   return jwt.sign({ ...data }, process.env.JWT_SECRET, {
//     expiresIn: expiresIn,
//   });
// };
